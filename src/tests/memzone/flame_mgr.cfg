log_dir = .

msger_id = 127.0.0.1/12345

msg_worker_num = 1

# msg config
# rdma_enable
# true/false
rdma_enable = true

# rdma_device_name
# @example: mlx5_0     (from commond ibv_devices)
rdma_device_name = mlx5_0

# rdma_port_num
# 1~255
rdma_port_num = 1

# rdma_path_mtu
# 256,512,1024,2048,4096
rdma_path_mtu = 4096

# rdma_enable_srq
# true/false
rdma_enable_srq = true

# rdma_cq_pair_num
# < msg_manager.worker_num
rdma_cq_pair_num = 1

# rdma_traffic_class
# 0~255
rdma_traffic_class = 0

# rdma_enable_hugepage
# true/false
rdma_enable_hugepage = true

# rdma_hugepage_size
# 2M, 8M, 1G
#rdma_hugepage_size = 2M

# rdma_mem_min_level
# RDMA RdmaBuffer min size (2^min_level)
#rdma_mem_min_level = 12

# rdma_mem_max_level
# RDMA RdmaBuffer max size (2^max_level).
# One BuddyAllocator will have 2^max_level bytes.
#rdma_mem_max_level = 28